# Sample Videos

Drop screen recordings here for testing clipsheet locally.

Supported formats: `.mp4`, `.mov`, `.webm`, `.mkv`, `.avi`

Video files in this directory are gitignored (too large for version control).

## Quick test

```bash
clipsheet samples/your-recording.mp4 -o /tmp/clips -v
```
